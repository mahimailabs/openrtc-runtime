from __future__ import annotations

import logging

logger = logging.getLogger("openrtc")


def main() -> int:
    """Placeholder CLI for the package entrypoint.

    Milestone 1 focuses on the programmatic API. The full discovery-based CLI is
    planned for Milestone 2.
    """
    logger.info("OpenRTC CLI discovery commands are planned for Milestone 2.")
    return 0
